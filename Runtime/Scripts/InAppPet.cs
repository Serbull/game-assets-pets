using TMPro;
using UnityEngine;

public class InAppPet : MonoBehaviour
{
    //[YaGamesSDK.InAppId][SerializeField] private string _id;
    //[SerializeField] private int _location;
    //[SerializeField] private TextMeshProUGUI _price;

    //[HideInInspector] public Sprite Sprite;
    //[HideInInspector] public int Value;
    //public string Id => _id;

    //private void Start()
    //{
    //    if (SaveManager.Data.location != _location)
    //    {
    //        gameObject.SetActive(false);
    //        return;
    //    }

    //    var index = Configs.Instance.PetConfig.GetPetIndex(_id);
    //    _price.text = "X" + Configs.Instance.PetConfig.GetPetData(index).SpeedBonus.ToShortValue();

    //    Sprite = Configs.Instance.PetConfig.GetPetData(index)._prefab.Icon;
    //    Value = (int)Configs.Instance.PetConfig.GetPetData(index).SpeedBonus;
    //}
}
